programa {
  funcao inicio() {
  
   real base
   real altura
   real area

   escreva("o valor da base é ? ")
   leia(base)
   escreva("o valor da altura é ? ")
   leia(altura)

   area = (base*altura) /2
   

   escreva("o valor da area é ", area )
  }
}
